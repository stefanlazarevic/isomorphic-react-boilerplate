import styled from 'styled-components';
import { hot } from 'react-hot-loader';
import MenuButton from './MenuButton';

const StyledMenuButton = styled(MenuButton)``;

export default hot(module)(StyledMenuButton);
